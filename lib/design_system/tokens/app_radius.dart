import 'package:flutter/cupertino.dart';

abstract final class AppRadius {
  static const radiusXS = BorderRadius.all(Radius.circular(8.0));
  static const radiusSM = BorderRadius.all(Radius.circular(12.0));
  static const radiusMD = BorderRadius.all(Radius.circular(16.0));
  static const radiusLG = BorderRadius.all(Radius.circular(24.0));
  static const radiusXL = BorderRadius.all(Radius.circular(32.0));
  static const radiusFull = BorderRadius.all(Radius.circular(999.0));

  static const double xs = 8.0;
  static const double sm = 12.0;
  static const double md = 16.0;
  static const double lg = 24.0;
  static const double xl = 32.0;
  static const double full = 999.0;
}
